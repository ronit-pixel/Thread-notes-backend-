<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-700 text-white py-3 px-10">
        <div class="flex justify-end">
            <a href="/logout" class="px-3 py-2 text-2xl bg-red-500 rounded-md">logout</a>
        </div>
        <div class=" w-1/4 flex justify-center item-start  gap-3 px-0 m-0">
            <div class="w-14 h-14 rounded-full"> 
                <img src="/images/uploads/<%= user.profilepic%>" class="w-full h-full object-cover rounded-full" alt="">
            </div>
            <h3 class="text-2xl tracking-tight pt-3">Hello, <%= user.name%> 👋 </h3>
        </div>
        <h3 class="text-xl tracking-tight pt-2">You can create your post.</h3>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your Mind?"
                class="p-4 block mt-1 outline-none resize-none bg-transparent border-2 border-zinc-500 w-1/3 rounded-md"
                id=""></textarea>
            <input type="submit" value="submit" class="py-2 px-3 bg-blue-600 mt-3 rounded-md">
        </form>

        <div class=" post mt-16">
            <h3 class="text-md">Your Post....</h3>
        </div>
        <div class="postcontainer">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-3 border-2 border-zinc-500 bg-zinc-600 rounded-md">
                    <h4 class="font-medium text-2xl text-blue-500">
                        <%= user.username %>
                    </h4>
                    <p class="tracking-tight text-sm">
                        <%= post.content%>
                    </p>
                    <div class="flex gap-4">
                        <small class="pt-1">
                            <%= post.likes.length %> Like
                        </small>
                        <a href="/like/<%= post._id%>" class="text-blue-500">
                            <%= post.likes.indexOf(user._id) ? "Like" : "Unlike" %>
                        </a>
                        <a href="/edit/<%= post._id%>">edit</a>
                    </div>
                </div>
                <% })%>
        </div>

    </div>
</body>

</html>